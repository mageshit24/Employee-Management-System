<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<!-- 3.5.16 rather than the latest Spring Boot 4.x: it's the final,
		     Java-25-ready patch on the 3.5.x line this project already
		     targets. Spring Boot only certified Java 25 support from 3.5.5
		     onward (3.5.4 predates it) - 4.x brings breaking changes
		     (Jackson 3, a JakartaEE 11 baseline, split starter jars) that
		     are out of scope for a JDK bump alone. -->
		<version>3.5.16</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>magesh.ems</groupId>
	<artifactId>backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>backend</name>
	<description>Employee Management using MySQL</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<!-- JDK 25 (LTS, released Sept 2025). Spring Boot reads this
		     property to set both the Maven compiler's "release" flag and
		     the packaged jar's target bytecode version - one property
		     instead of configuring the compiler plugin by hand. -->
		<java.version>25</java.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<!-- Local-development convenience only: auto-restarts the app when
		     compiled classes change, and enables LiveReload. `optional`
		     keeps it off the classpath of anything that depends on this
		     module, and spring-boot-maven-plugin excludes optional
		     dependencies from the repackaged jar by default - so this
		     never ships to a deployed/production build. DevTools also
		     self-disables automatically when run from a repackaged jar
		     (java -jar ...) rather than an exploded classpath, which is a
		     second, independent guard against it turning up in prod. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<optional>true</optional>
		</dependency>

		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<!-- Keeps real parameter names (employeeId, sortBy, ...)
					     in the compiled bytecode instead of javac's default
					     arg0/arg1/arg2. Mainly a development/debugging aid -
					     it's what makes a debugger or a stack trace show the
					     actual parameter name - and Spring MVC also relies
					     on it in a few places to resolve @RequestParam /
					     @PathVariable names without needing them spelled out
					     explicitly, though this project already spells them
					     out everywhere for clarity regardless. -->
					<parameters>true</parameters>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<!-- Both are optional dependencies (see above), which
					     spring-boot-maven-plugin already excludes from the
					     repackaged jar by default - these entries just make
					     that exclusion explicit and easy to find, rather
					     than relying on the default silently doing the
					     right thing. -->
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
						<exclude>
							<groupId>org.springframework.boot</groupId>
							<artifactId>spring-boot-devtools</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
